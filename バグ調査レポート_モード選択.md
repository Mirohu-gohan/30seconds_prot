# バグ調査レポート：モード選択 & ステージ選択の混乱

**調査日**: 2026-05-11  
**ブランチ**: Kawabata_Meteor  
**調査対象**: `Assets/Prefabs/UI/StageSelect.prefab`、`ModeSetting.cs`、`StageSetting.cs`、`Next.cs`

---

## 結論（一言で）

> **ModeLeft・ModeRight・StageLeft・StageRight の各ボタンがそれぞれ"自分専用のモード状態"を持っており、互いに通信していない。**

---

## 現状の構造（なにがおかしいか）

### 本来あるべき姿

```
[ StageSelectManager（1つの管理役） ]
  ModeSetting  ← モードの状態をここ1か所で管理
  StageSetting ← ステージの状態をここ1か所で管理

ModeLeft ボタン ─┐
ModeRight ボタン ─┤→ 全部、上の Manager を呼ぶ
StageLeft ボタン ─┤
StageRight ボタン ┘
```

### 現実の姿（バグの根本）

```
ModeLeft ボタン  ─── ModeSetting #1 ───→ StageSetting #2（StageLeft上）
ModeRight ボタン ─── ModeSetting #2 ───→ StageSetting #1（StageRight上）
StageLeft ボタン ─── StageSetting #2 ─── Ms: ModeSetting #1（ModeLeft上）
StageRight ボタン ── StageSetting #1 ─── Ms: ModeSetting #2（ModeRight上）
```

**各ボタンが「自分自身のコンポーネント」に対して処理を呼んでいる。**  
ModeLeft と ModeRight はそれぞれ独立した "currentMode" を持っており、どちらを押しても相手には伝わらない。

---

## 発見したバグ 3つ

---

### バグ① 【最重大】モード状態が2つに分裂している

#### 何が起きているか

| ボタン | 呼ぶコンポーネント | 参照するモード状態 |
|--------|------------------|-----------------|
| ModeLeft | ModeSetting **#1** | currentMode A |
| ModeRight | ModeSetting **#2** | currentMode B |
| StageLeft | StageSetting **#2** → Ms = ModeSetting **#1** | currentMode A |
| StageRight | StageSetting **#1** → Ms = ModeSetting **#2** | currentMode B |

#### 再現手順

```
① 起動直後
   MS#1 = Survival、MS#2 = Survival
   UI表示: 「対戦モード」

② ModeRight（→）を押す
   MS#2 が Score に切り替わる
   UI表示: 「スコアモード」✓（一見正常）

③ StageLeft（←）を押す
   StageLeft は StageSetting#2 → ModeSetting#1 を読む
   MS#1 は まだ Survival のまま！
   → Survival のステージ一覧で前に戻る
   UI表示: 「スコアモード」なのに「サバイバルのステージ」が表示される ← バグ発生

④ スタートボタンを押す
   Next.cs が参照する Ms/Ss 次第で、
   「画面に表示されているステージ」と「実際に読み込まれるシーン」がズレる
```

#### 原因（プレハブYAML）

`ModeSetting` と `StageSetting` が各ボタンオブジェクト自身にアタッチされてしまっている。

```
// StageSelect.prefab（一部抜粋）

--- GameObject: ModeLeft
  components:
    - ModeSetting #1   ← ここに付いているのがおかしい

--- GameObject: ModeRight
  components:
    - ModeSetting #2   ← ここにも別のが付いている

--- GameObject: StageRight
  components:
    - StageSetting #1  ← ボタンにステージ管理がいる

--- GameObject: StageLeft
  components:
    - StageSetting #2  ← ここにも別のがいる
```

---

### バグ② `PreviousMode` が `NextMode` と同じ処理になっている

#### 該当箇所

`ModeSetting.cs` 27行目

```csharp
// NextMode（右ボタン）
public void NextMode()
{
    currentMode = (GameMode)(Mathf.CeilToInt((int)currentMode + 1) % 2);
    UpdateModeText();
}

// PreviousMode（左ボタン）← NextMode と一字一句同じ！
public void PreviousMode()
{
    currentMode = (GameMode)(Mathf.CeilToInt((int)currentMode + 1) % 2);  // ← バグ
    UpdateModeText();
}
```

#### 現状の挙動

モードが Survival / Score の **2種類しかない** ため、どちら向きに押しても同じトグルになり、今は実害が出ていない。  
しかし将来モードを3種類以上に増やした瞬間に「左を押したのに右に進む」バグが顕在化する。

---

### バグ③ ステージ選択にテストシーンが混入している

#### 該当データ（StageSetting #1 と #2、両方に同様の内容）

```
Survive_stages（サバイバルモード）:
  ステージ１: Gimmick_Meteor    ← ギミックテスト用シーン
  ステージ２: Gimmick_Landmine  ← ギミックテスト用シーン
  ステージ３: 暴走トロッコ

Score_stages（スコアモード）:
  ステージ１: Meteor_ver2
  ステージ２: 暴走トロッコ
  ステージ３: Gimmick_Landmine  ← ギミックテスト用シーンがここにも
```

`Gimmick_Meteor` と `Gimmick_Landmine` は開発中のテストシーン。  
これが「**モード選択画面にないはずの画面**」の正体と考えられる。  
プレイヤーがこのステージを選んでスタートすると、テスト用シーンに飛んでしまう。

---

## 改善方法

### 修正① 【最重要】Manager オブジェクトに集約する

**やること**: 空の GameObject `StageSelectManager` を作り、`ModeSetting` と `StageSetting` を **1個ずつ** そこに移す。各ボタンの onClick はすべてその1つを参照するよう付け直す。

**手順**:

1. Hierarchy で `StageSelect` シーンに空の `GameObject` を作り、名前を `StageSelectManager` にする
2. `ModeLeft` にある `ModeSetting` コンポーネントを `StageSelectManager` に **移す**（Cut & Paste）
3. `ModeRight` にある `ModeSetting` コンポーネントを **削除**
4. `StageLeft` にある `StageSetting` コンポーネントを `StageSelectManager` に **移す**
5. `StageRight` にある `StageSetting` コンポーネントを **削除**
6. 各ボタンの `OnClick` を `StageSelectManager` 上の1つのインスタンスに向け直す：

```
ModeLeft.OnClick  → StageSelectManager/ModeSetting.PreviousMode()
ModeRight.OnClick → StageSelectManager/ModeSetting.NextMode()
StageLeft.OnClick → StageSelectManager/StageSetting.PreviousStage()
StageRight.OnClick→ StageSelectManager/StageSetting.NextStage()
NextScene.OnClick → StageSelectManager/Next.NextScene()
```

7. `StageSetting.Ms` と `Next.Ms`・`Next.Ss` の参照もすべて `StageSelectManager` のものに更新する

---

### 修正② `PreviousMode` のロジックを直す

`ModeSetting.cs` 27行目を修正：

```csharp
// 修正前
public void PreviousMode()
{
    currentMode = (GameMode)(Mathf.CeilToInt((int)currentMode + 1) % 2);
    UpdateModeText();
}

// 修正後
public void PreviousMode()
{
    int count = System.Enum.GetValues(typeof(GameMode)).Length;
    currentMode = (GameMode)((int)currentMode - 1 + count) % count;
    UpdateModeText();
}
```

これにより、将来モードが増えても正しく「前に戻る」動作になる。

---

### 修正③ テストシーンをステージ一覧から外す

`StageSelectManager` の `StageSetting` を Inspector で開き、`Survive_stages` から以下を削除：

- `Gimmick_Meteor`
- `Gimmick_Landmine`

`Score_stages` からも：

- `Gimmick_Landmine`

正式なステージシーンのみを登録し直す。

---

## 優先度まとめ

| # | 問題 | 深刻度 | 修正難易度 |
|---|------|--------|----------|
| ① | ModeSetting/StageSetting が分裂・クロス参照 | 🔴 致命的 | 中（Inspector作業） |
| ② | PreviousMode が NextMode と同じロジック | 🟡 潜在的 | 低（1行修正） |
| ③ | テストシーンがステージ一覧に混入 | 🔴 致命的 | 低（Inspector作業） |

修正①を行うだけで、モード混乱バグの大部分は解消される。
